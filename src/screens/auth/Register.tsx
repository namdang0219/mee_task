import { View, Text } from "react-native";
import React from "react";
import { SafeView } from "components/layouts";

const Register = () => {
	return (
		<SafeView><Text>Register</Text></SafeView>
	);
};

export default Register;
